# API distante (HTTPS recommandé — HTTP provoque une redirection qui casse le preflight CORS)
VITE_API_BASE_URL=https://nano-orbite.tlugagne.live

# Proxy Vite optionnel si VITE_API_BASE_URL est vide
VITE_API_PROXY_TARGET=http://127.0.0.1:5000
